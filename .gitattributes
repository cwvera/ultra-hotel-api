# Default: normalize to LF on commit
* text=auto eol=lf

# Shell scripts must always be LF (Docker/Linux containers)
*.sh text eol=lf

# Windows-specific files can use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
